apiVersion: v1
kind: Service
metadata:
  name: kubectl-command-tool-server
spec:
  type: ClusterIP
  selector:
    app: kubectl-command-tool-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
